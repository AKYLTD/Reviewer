# Square integration (production)
# See square-setup-plan.md for the full configuration. None of these are
# required in development — the catering route is a capture stub locally.
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=sandbox

# Optional: forward catering enquiries to this address (POST /api/catering)
CATERING_ENQUIRY_EMAIL=
